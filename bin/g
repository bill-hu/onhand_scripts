#!/usr/bin/python2.6 
#cording=utf-8
import sys,os
import shutil
import filecmp
from os.path import walk, join, normpath
targetpath=""
srcpath=""
backuppath=""
identifier="--"

if len(sys.argv) <3:
     print "Usage: "+sys.argv[0]+" Keyword "+ "filetype" 
     exit(-1)
cmd="find . -type f -name \""+sys.argv[2]+"\"|xargs grep -n \""+sys.argv[1]+"\""
print cmd
os.system(cmd);
